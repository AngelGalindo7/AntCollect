# Python
__pycache__/
*.pyc
.pytest_cache/
venv/
antcollect/

# Environment / secrets
.env
.env.local
frontend/e2e/.env.test

# Scratch / AI session notes
gemini.md

# Java build output
messaging-service/target/

# Uploads (user-generated content)
Uploads/

# Logs
*.log

# Local dev notes
postgresqlsetup.txt
requirements.txt
table_creation.txt

# Dev-only session references (never commit)
CLAUDE.md
PROJECT_MAP.md
docs/

# OS
.DS_Store

# LocalStack data (ephemeral runtime state — recreated by test-db-setup.sh)
localstack_data/

# Node (root-level, not frontend — prevents accidentally committing a root npm install)
node_modules/

# NotebookLM export script and output (local dev tooling, not project source)
export_for_notebooklm.py
notebooklm/

# Scratch / work-in-progress scripts not yet ready to commit
run-tests.sh
